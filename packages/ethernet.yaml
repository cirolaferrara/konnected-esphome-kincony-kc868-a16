ethernet:
  type: LAN8720
  mdc_pin: GPIO23
  mdio_pin: GPIO18
  clk:
    mode: CLK_OUT
    pin: 17
  phy_addr: 0

text_sensor:
  - platform: ethernet_info
    ip_address:
      id: ip_address
      name: Ethernet IP Address
      entity_category: diagnostic
