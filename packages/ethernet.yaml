ethernet:
  type: LAN8720
  mdc_pin: GPIO23
  mdio_pin: GPIO18
  clk_mode: GPIO17_OUT
  phy_addr: 0

text_sensor:
  - platform: ethernet_info
    ip_address:
      id: ip_address
      name: Ethernet IP Address
      entity_category: diagnostic